[/Script/ModelContextProtocolEngine.ModelContextProtocolSettings]
; Arranca el servidor MCP del editor solo, en cada apertura del proyecto.
; Sin esto hay que escribir ModelContextProtocol.StartServer en la consola
; cada vez, o lanzar el editor con -ModelContextProtocolStartServer.
bAutoStartServer=True
ServerPortNumber=8000
ServerUrlPath=/mcp
