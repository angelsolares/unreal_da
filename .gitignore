# ---------------------------------------------------------------
# Assets de TERCEROS — NO se suben (licencia comercial)
# ---------------------------------------------------------------
# Dynamic Combat System y GiantBossProject son assets de pago.
# Redistribuirlos, y menos en un repo publico, viola su licencia.
# Cada persona que clone el proyecto debe instalarlos desde su
# propia cuenta de Fab / Epic.
Content/DynamicCombatSystem/
Content/GiantBossProject/

# ---------------------------------------------------------------
# Carpetas generadas por Unreal — se recrean solas
# ---------------------------------------------------------------
Binaries/
Build/
DerivedDataCache/
Intermediate/
Saved/
.vs/
*.VC.db
*.opensdf
*.opendb
*.sdf
*.sln
*.suo
*.xcodeproj
*.xcworkspace

# Copias de seguridad locales: git cumple ya esa funcion
_Backups/

# Compilados
*.dll
*.exe
*.lib
*.pdb
*.so
*.dylib

# ---------------------------------------------------------------
# Assets binarios grandes — descomentar si se activa Git LFS
# ---------------------------------------------------------------
# *.umap filter=lfs diff=lfs merge=lfs -text
# *.uasset filter=lfs diff=lfs merge=lfs -text

# Montages AM_DA_* : son DUPLICADOS de assets de pago.
#   Boss/   <- GiantBossProject/Anims/Attacks/  (+ ANS_HitBox a mano)
#   Player/ <- DynamicCombatSystem M_Backstabbed (derribo del jugador)
# Aunque solo llevan slot/tiempos/notifies y los keyframes siguen en los packs,
# son assets derivados de contenido comercial. Misma regla que arriba.
# Como recrearlos: ver DarkAngels_POC_Notas.md.
Content/DarkAngels/Animations/
